import 'package:flutter/material.dart';

class ReportUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('ReportUpload')),
      body: Center(child: Text('Upload medical reports...')),
    );
  }
}
